# RustDesk Server Configuration
# Replace with your server's IP or domain
RUSTDESK_RELAY_HOST=202.61.192.237

# Y = Always use relay (more reliable, uses more bandwidth)
# N = Try P2P first, fallback to relay (default)
ALWAYS_USE_RELAY=N
